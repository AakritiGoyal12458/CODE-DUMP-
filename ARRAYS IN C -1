/******************************************************************************

                            Online C Compiler.
                Code, Compile, Run and Debug C program online.
Write your code in this editor and press "Run" button to compile and execute it.

*******************************************************************************/

#include <stdio.h>

int main()
{
    int array[5];
    // array[0]=1;
    // array[1]=2;
    //1,2,3,4,5
    for(int i = 0 ; i<5 ; i++){
       array[i]=i+1;
    }
    int array[5]={1,3,3,4,50};
    
    //int array[]={1,2,3,4,5};
    // for(int i = 0 ; i<5 ; i++){
    //     printf("%d \n",array[i]);
    // }
    
    int n = sizeof(array);
    printf("the size of array is %d",n);
    
    //search an element in  an array:present or not present
    //find minimum element
    //Finding count of an element in an array
    int count = 0;
    int key=3;
    for(int i = 0 ; i<5 ; i++){
      if (arr[i]==key){ 
        count ++   ; 
      }
    }
    printf("the count of %d is %d",key,count)
    /*In a university 10 students are there. WAP to:
    store their roll number and marks using array. 
    (only in for loop : scanf )
    Given roll number of a student, find his marks.*/
    return 0;
}
